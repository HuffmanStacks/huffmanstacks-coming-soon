# Force LF on everything that runs on the Linux VPS, so CRLF from a Windows
# checkout (core.autocrlf=true) never breaks a shebang or systemd unit.
*.sh            text eol=lf
*.service       text eol=lf
*.timer         text eol=lf
robots.txt      text eol=lf
